<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.annef.workoutchrono" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ico_chrono"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.WorkoutListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ExerciseActivity"
            android:label="Exercises">
        </activity>
        <activity
            android:name=".activity.WorkoutActivity"
            android:label="Workout" >
        </activity>
        <activity
            android:name=".activity.ChronoActivity"
            android:label="Chrono" >
        </activity>
        <activity
            android:name=".activity.PickNumberActivity"
            android:label="@string/title_activity_pick_number"
            android:theme="@android:style/Theme.Holo.Dialog" >
        </activity>
        <activity
            android:name=".activity.PickTimeActivity"
            android:label="@string/title_activity_pick_time"
            android:theme="@android:style/Theme.Holo.Dialog" >
         </activity>
        <activity
            android:name=".activity.PickTypeActivity"
            android:label="@string/title_activity_pick_time"
            android:theme="@android:style/Theme.Holo.Dialog" >
        </activity>
    </application>
    <uses-feature android:name="android.hardware.camera"
        android:required="false" />

</manifest>
